<li>
    <a href="{{ path('cms_dashboard', { _want_to_be_this_user: '_exit' }) }}" class="exit_impersonating">
        <i class="fa fa-mail-reply"></i>
        {% if previousToken %}
            {% trans %}Go back to{% endtrans %} <strong>{{ previousToken.user.username }}</strong>
        {% else %}
            {% trans %}Exit impersonation{% endtrans %}
        {% endif %}
    </a>
</li>
<li class="divider"></li>