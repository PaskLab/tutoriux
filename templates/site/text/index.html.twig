{% extends 'site/layout.html.twig' %}

{% block content_title %}
    <h1>
        {{ section }}
        {% if section.metaDescription is not empty %}
            <small>"{{ section.metaDescription }}"</small>
        {% endif %}
    </h1>
{% endblock %}

{% block content %}
    {{ render_esi(controller('App\\Controller\\Site\\TextController::displayTexts')) }}
{% endblock %}